# Copie para .env e ajuste se necessário. Os defaults batem com o docker-compose.
PG_HOST=localhost
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=postgres
PG_DATABASE=pgqueue

PGQUEUE_NAME=demo_queue
PGQUEUE_VT=30
PGQUEUE_POLL_INTERVAL=1.0
